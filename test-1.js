// 1) Observe o trecho de código:

let INDICE = 12;
let SOMA = 0;
let K = 1;

while (K < INDICE) {
    K = K + 1;
    SOMA = SOMA + K;
}

console.log(SOMA); // Ao final do processamento, qual será o valor da variável SOMA?
// Resposta: 77

